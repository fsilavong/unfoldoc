# Hugging Face Papers Digest

This source file uses an `orbit` block to declare how the digest artifact is produced and which markdown artifact should be rendered.

```orbit
run: ./scripts/make_digest.sh
artifact: artifacts/hf-digest/content.md
```
